{
  "openapi": "3.0.0",
  "info": {
    "title": "Francine’s Recipe Vault API",
    "version": "1.0.0"
  },
  "paths": {
    "/api": {
      "get": {
        "summary": "Test endpoint",
        "description": "Checks if the plugin server is running.",
        "operationId": "testEndpoint",
        "responses": {
          "200": {
            "description": "A simple response indicating the server is running"
          }
        }
      }
    }
  }
}
